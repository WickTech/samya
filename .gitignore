# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.key
*.p8
*.p12
*.pfx
*-service-account*.json
secrets.json
*:Zone.Identifier

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — ignore every variant, keep only the committed template
.env
.env.*
!.env.example

# local admin data store (dev fallback when no KV is configured) — holds
# real customer orders + WhatsApp numbers once used
/.data
admin-store.json

# vercel
.vercel

# typescript
*.tsbuildinfo
